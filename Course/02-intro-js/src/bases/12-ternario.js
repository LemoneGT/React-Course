const activo = true;

// let mensaje = '';

// if(activo){
//     mensaje = 'activo';
// }else{
//     mensaje = 'inactivo';
// }

// const mensaje = (activo) ? 'Activo' : 'Inactivo';
// const mensaje = (activo) ? 'Activo' : null;

// short code to make if
const mensaje = !activo && 'Activo'

console.log(mensaje)